<!DOCTYPE html>
<html>
    <head>
        <title>Weather app</title>
        <link rel="stylesheet" type="text/css" href="/css/style.css">
        <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
    </head>
    <body>
        <div class="topnav">
            <a class="active" href="#home">Home</a>
            <a href="#about">Your Account</a>
            <a href="#contact">Sign Up</a>
        </div>
        <div class="search">
            <form action="/" method="POST">
                <!-- Create search bar with text already in it -->
                <input name="city" type="text" placeholder="Enter a City" required>
            </form>
            
        <% if(weather !== null){ %> <!-- if user enters valid weather lcoation -->
            <p><%= weather %></p>
        <% } %>

        <% if(error !== null){ %> <!-- if error isn't equal to nothing (if there's an error) -->
            <p><%= error %></p>
        <% } %>
            
        </div>
        <div class="search">
            <form action="/weather">
                <!-- Create weather button to automatically take user to weather page -->
                <button><i class="weatherButton"></i>Weather</button>
            </form>
        </div>
    </body>
</html>  